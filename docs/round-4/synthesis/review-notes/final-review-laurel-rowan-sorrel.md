# Final semantic review: Laurel / Rowan / Sorrel

Reviewed the complete current round-4 synthesis draft, with particular attention to the finite support proof, projection formula, request sealing, profiles, behavioral mathematics, evidence qualifications and evaluation. No tests/builds or TeX edits performed. Root is concurrently revising, so draft line anchors below refer to the initial reviewed draft; the quoted subsection/claim identifies each location after shifts.

## Corrections to make

1. **Define the total-support universe before the residual projection formula.** In “Total supports, missing supports, and author choices” (initial draft405–412), `Req_K` was defined using a fixed offered universe O, but the new total family S(q) is undefined. The formula is not justified merely by subtracting K from an arbitrary family over O. For instance, K={A}, O={B}, and A→q gives `Req_K(q)={∅}`, whereas a family derived from offered B alone is empty.

   One precise repair preserving the earlier definition is: put `H=K∪O`; define `L_H(q)=Min⊆{S⊆H : q∈Cl_R(S)}` with no pre-discharged facts; then `Req_K(q)=Min⊆{S\K:S∈L_H(q)}`. Every minimal offered support excludes K, so overlap of K and O causes no problem. For reusable availability updates, instead fix authorized H once, vary `K⊆H`, and use `O=H\K`. State that the profile/atom meanings/rules/method policy remain fixed. Sorrel proves exactly this latter form in `docs/round-4/ideas/Sorrel/sorrel.tex:700` and `:817`; its theorem826–836 gives both directions for added `U⊆H\K`.

2. **Keep the actual nonvacuous Frey fixture.** In “Exact quotients” (initial draft691–702), the draft invented `a^p+b^p=c^p` as part of the full coefficient benchmark and reinterpreted `(3,2,5)` as `(a,b,c)`. The reports use `(a,b,p)=(3,2,5)` to satisfy the four side conditions `2|b`, `p≥4`, p odd, and `a≡3 mod4`; no c or Fermat balance equation belongs in this fixture. Remove that detour. The a₄ exact-divisibility route uses only even b and the exponent bound; the selected a₂ route additionally uses odd p and the residue. The ambient Fermat package can require other premises, but they should not be imported into this use-site test. Source: `Laurel/Laurel.tex:554`, `Rowan/Rowan.tex:979`, `Sorrel/sorrel.tex:1081`. This correction was already sent directly to root.

3. **Do not conflate CDF endpoint conventions with dropping μ-atoms.** “Almost-everywhere reasoning” (initial draft735–739), together with the Sorrel profile941–942, says Sorrel emphasizes atomic contributions but does not give its actual contract. Strict/non-strict cumulative functions can differ at atoms of μ, while their subsequent Lebesgue dt integrals agree. The fiber/outer measure must be identified before claiming that a point mass makes an endpoint change visible.

   A compact self-contained insertion is the finite-positive-measure formula: for μ a.e. supported on `[a,b]`, `a≤b`, z outside the complexified interval, finite real mass M=μ(R), and `Fμ(t)=μ((-∞,t])`,

   `∫ (z-x)⁻¹ dμ(x) = M/(z-b) − ∫_a^b Fμ(t)/(z-t)^2 dt`.

   Explain that endpoint changes are Lebesgue-null in t and do not discard atomic μ mass. The point-mass, mass-two and a=b fixtures distinguish atomicity, normalization and degeneracy. The exact a.e. integral-congruence consumer does not require integrability, while integral subtraction or Fubini have their own premises. All are explicit in `Sorrel/sorrel.tex:954`, `:974`, `:993`, `:1061`. No new Lean theorem is claimed by including this mathematical reconstruction.

## Valuable additions currently missing or understated

4. **Retain Laurel's executed lost-premise repair.** The Laurel profile (initial draft904–918) currently retains generic support tests, cycles, width and partiality but omits one of its strongest distinct contributions. `reopen_known` in `docs/round-4/ideas/Laurel/prototype/laurel_core.py:179` rebuilds a selected proof after removal of known facts. It converts only actually used removed facts into offered residuals, creates a new profile identity and independently checks the reconstructed proof. Tests at `prototype/test_core.py:139` and `:148` cover used and unused removals. The proposition and exact limits are at `Laurel.tex:425–436`.

   Add a paragraph under edit lifetime or the profile: from a fixed-profile derivation `Γ,h:P ⊢ G`, abstraction produces `Γ ⊢ P→G`. This preserves the old route and gives a sufficient, possibly nonminimal repair. A fresh support search might find a different method with fewer premises. The implementation is symbolic and does not establish arbitrary dependent Lean context migration. My earlier lane memo's cautious word “proposes” understates the executed symbolic operation; this final review corrects that status.

5. **Credit Laurel's realized affine-span criterion alongside Alder's integer frame.** The behavioral section correctly distinguishes integer spans from rational spans, but Laurel's unique contribution is absent from its profile. Its theorem uses actual admissible witnesses whose lifted observations `(1,o(x))` span every admissible lift over Q (`Laurel.tex:468–515`). Finite basis existence gives at most d+1 witnesses, not an algorithm to discover source coverage. Add a short cross-reference in the semilinear comparison: Rowan's bases and one-generator extensions form an explicit test family, and rational linear algebra can compress it when a witnessed coverage theorem is preserved. Keep the present torsion warning; a field-span argument is not a replacement for the integer/abelian-group theorem.

6. **State the sealing disagreement concretely.** “A fixed request may still contain legitimate parameters” (initial draft262–264) says the reports give variants but does not identify them. Rowan requires the target and every local declaration's type/value, including universe parameters, to have no unresolved metavariables before dispatch (`Rowan.tex:560–601`). Laurel/Sorrel propose classifying allowed proof or construction holes versus meaning-bearing holes (`Laurel.tex:417–423`; `Sorrel/sorrel.tex:637–655`). Both permit fixed free variables and constructing a witness for a fixed existential goal. State strict sealing as an initially testable policy, and ask which real Lean case would require a principled relaxation.

7. **Make publication debt explicit.** The common semantics checks requested targets, but the draft never clearly states that every assertion displayed as checked needs evidence, including an assertion unused by the final theorem. One sentence in completion fidelity plus a nearby-positive mutation closes this gap: an unused false intermediate statement cannot disappear from publication checks merely because Lean's final proof term ignores it. Sources: `Laurel.tex:758–761`, `Sorrel/sorrel.tex:1619`, and Sorrel's accepted-artifact schema near1771.

8. **Restore three concrete evaluation safeguards.** The matched controls and semantic mutations are good. Add that the Frey/Fabius/ODE/Cauchy/tensor cases already inspected are development tasks; an independent assessor selects held-out transfer tasks after the registry/interface freeze. Charge registration and helper-writing time explicitly, including work used by a single example. Set quantitative stop thresholds after a variance-estimating pilot and before the main comparison. These are stronger and more testable than qualitative “predeclare a measurable advantage.” Sources: `Laurel.tex:849–865`, `Rowan.tex:1931–1993`, `Sorrel/sorrel.tex:1557–1634`.

## Wording already correct and worth preserving

- The finite antichain theorem is sound for the specified ground Horn profile. Its upward-closure termination argument, proof-tree completeness argument, schedule qualification and distinction from verified Python are appropriate. Nullary rules correctly need actual evidence in a mathematical registry.
- The partial-result correction accurately distinguishes sound discovered proofs from a literal subset of the final minimal family. Keep the three completeness boundaries: saturation, bounded grounding coverage, and mathematical decision completeness.
- Rowan's cyclic leaf probe is accurately described as a diagnostic limitation, not a false accepted derived proof. Sorrel's selected-route mismatch is likewise a valid conditional theorem disconnected from an available completion.
- The method-policy paragraph correctly distinguishes forbidden-rule filtering from positive method requirements, which need method-sensitive state or separate profiles. It also avoids equating rule occurrence with indispensability.
- Conditional templates, concrete mathematical registrations and the theorem-free Laurel cycle module are separated correctly. The current compiler placeholder is root's responsibility; I did not verify fresh Lean totals in this pass.
- The integer affine-frame theorem and semilinear criterion are mathematically sound as stated. Actual admissibility of the test inputs, exact candidate denotation, field-versus-group coefficient hypotheses and negative realization remain visible.
